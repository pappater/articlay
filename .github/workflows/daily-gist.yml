name: Magazine Scraper to Gist (Every 15 Minutes)

on:
  schedule:
    - cron: '*/15 * * * *'  # Every 15 minutes
  workflow_dispatch:

jobs:
  scrape-and-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run daily_gist_job.py
        env:
          GITHUB_TOKEN: ${{ secrets.GIST_TOKEN }}
        run: python daily_gist_job.py
